{
  "name": "catherine-lu-portfolio",
  "private": true,
  "version": "1.0.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "check": "node --check script.js && node scripts/check.mjs",
    "build": "node build.mjs"
  }
}
